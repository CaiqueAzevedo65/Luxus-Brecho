# CONFIGURAÇÃO MONGODB ATLAS
# URI completa de conexão para MongoDB Atlas
# Substitua <USERNAME>, <PASSWORD> e <CLUSTER_URL> pelos valores do seu cluster Atlas
MONGODB_URI=mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER_URL>/?retryWrites=true&w=majority&appName=LuxusBrecho

# Nome do banco de dados a ser utilizado pela aplicação
MONGODB_DATABASE=nomedoseubanco

# Flask
FLASK_DEBUG=False

# CORS - origens permitidas do frontend (se não definido, usa localhost:5173)
# Pode receber múltiplas origens separadas por vírgulas
# FRONTEND_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
# Em produção, defina o domínio do frontend e mobile
# FRONTEND_ORIGIN=https://www.seu-dominio.com

# SUPABASE STORAGE
# URL do projeto Supabase (exemplo: https://xyzcompany.supabase.co)
SUPABASE_URL=https://your-project.supabase.co
# Chave pública anon do Supabase
SUPABASE_KEY=your-anon-public-key-here
# Nome do bucket para imagens de produtos
SUPABASE_BUCKET=product-images

# EMAIL SMTP - Confirmação de email
# Host do servidor SMTP (exemplo: smtp.gmail.com)
SMTP_HOST=smtp.gmail.com
# Porta do servidor SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587
# Usuário/email do SMTP
SMTP_USER=seu-email@gmail.com
# Senha do SMTP (para Gmail, use App Password)
SMTP_PASSWORD=sua-senha-de-app
# Email remetente (se não definido, usa SMTP_USER)
FROM_EMAIL=seu-email@gmail.com
# Nome do remetente
FROM_NAME=Luxus Brechó
# URL base da aplicação (para links de confirmação)
APP_URL=http://localhost:5000