name: 🛡️ Security Tests

on:
  push:
    branches: [ main, dev, mobile ]
  pull_request:
    branches: [ main, dev ]
  schedule:
    - cron: '0 2 * * *'  # Todo dia às 2h da manhã

jobs:
  security-scan:
    name: Security Analysis
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
    
    - name: 🐍 Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'
    
    - name: 📦 Instalar dependências
      run: |
        cd backend
        pip install -r requirements.txt
        pip install requests  # Para o script de segurança
    
    - name: 🚀 Iniciar backend Flask
        run: |
          cd backend
          python app.py &
          echo $! > backend.pid
          
          # Aguardar backend inicializar
          echo "⏳ Aguardando backend..."
          for i in {1..30}; do
            if curl -f http://localhost:5000/api/health >/dev/null 2>&1; then
              echo "✅ Backend iniciado!"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "❌ Timeout: Backend não inicializou"
              exit 1
            fi
            sleep 2
          done
        env:
          FLASK_ENV: testing
        MONGODB_URI: ${{ secrets.MONGODB_URI || 'mongodb://localhost:27017/test' }}
    
    - name: 🛡️ Executar análise de segurança
      run: |
        python security-tests/security-analyzer.py
      continue-on-error: true  # Não falhar o job se houver vulnerabilidades
    
    - name: 📤 Upload dos relatórios
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: security-reports-${{ github.run_number }}
        path: security-reports/
        retention-days: 30
    
    - name: 📧 Enviar email com resultado
      uses: dawidd6/action-send-mail@v3
      if: always()
      with:
        server_address: ${{ secrets.SMTP_SERVER }}
        server_port: ${{ secrets.SMTP_PORT }}
        username: ${{ secrets.SMTP_USERNAME }}
        password: ${{ secrets.SMTP_PASSWORD }}
        subject: "🛡️ Relatório de Segurança - Luxus Brechó (Run #${{ github.run_number }})"
        to: ${{ secrets.EMAIL_TO }}
        from: ${{ secrets.EMAIL_FROM }}
        html_body: |
          <h2>🛡️ Relatório de Segurança - Luxus Brechó</h2>
          
          <p><strong>Data:</strong> ${{ steps.date.outputs.date }}</p>
          <p><strong>Branch:</strong> ${{ github.ref_name }}</p>
          <p><strong>Commit:</strong> ${{ github.sha }}</p>
          <p><strong>Status:</strong> ${{ job.status }}</p>
          
          <p>Os relatórios detalhados estão disponíveis nos artefatos do workflow:</p>
          <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">
            Ver detalhes no GitHub Actions
          </a>
          
          <p>Para baixar os relatórios:</p>
          <ol>
            <li>Clique no link acima</li>
            <li>Role até "Artifacts" na página</li>  
            <li>Baixe "security-reports-${{ github.run_number }}"</li>
          </ol>
          
          <hr>
          <p><em>Este é um email automático do sistema de CI/CD</em></p>
    
    - name: 🧹 Cleanup
      if: always()
      run: |
        if [ -f backend.pid ]; then
          kill $(cat backend.pid) 2>/dev/null || true
          rm backend.pid
        fi
